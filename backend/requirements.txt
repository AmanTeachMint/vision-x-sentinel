# Vision X Sentinel Backend Dependencies
# Python 3.10+ required

# Web Framework
Flask>=2.3.0
flask-cors>=4.0.0

# Database
pymongo>=4.6.0

# Environment Variables
python-dotenv>=1.0.0

# Computer Vision & AI
opencv-python>=4.8.0
numpy>=1.24.0
# Note: torch is installed separately via build command for CPU-only version
# ultralytics will install torch as dependency, but Railway build command installs CPU-only first
ultralytics>=8.0.0

# Note: PyTorch installation
# For Railway: CPU-only PyTorch installed via build command (~200MB vs ~2GB CUDA)
# For local CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu118
